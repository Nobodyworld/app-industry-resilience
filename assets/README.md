# Assets

Static assets used by the Streamlit UI and documentation live here (logos, icons, lightweight illustrations). Keep files small and version-friendly; large binaries belong in object storage or a CDN.

When adding a new asset, document its usage in the relevant UI module or guide so others can trace dependencies.
