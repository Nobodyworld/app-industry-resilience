# v0.2.0 Public Beta Release Candidate

**Status:** Phase 2 complete — Windows/Edge acceptance passed; GO recommended<br>
**Issue:** [#107](https://github.com/Nobodyworld/app-industry-resilience/issues/107)  
**Candidate branch:** `release/v0.2.0-rc1`  
**Starting main SHA:** `f98962ddd8e6490115cf2ac6b332a9ae86bbea76`  
**Initial accepted candidate SHA:** `9bf85d5054d01b028a2df05c28e281ee6b98b6e6`  
**Recovered blocker-fix SHA:** `6ef315903a92ff0ba23d939594dde0ad457c722b`  
**Windows/Edge accepted candidate SHA:** `9b8bc27c79500451414470e4364ed9137ed28d07`<br>
**Candidate package version:** `0.2.0rc1`  
**Intended final version/tag:** `0.2.0` / `v0.2.0`

## Release classification

This release remains **Public Beta**. The dashboard and API are suitable for reproducible demonstrations, comparative industry analysis, and portfolio review. The analytical ratios and composite indicators remain experimental and must not be interpreted as credit, insolvency, causal, investment, or policy conclusions.

The candidate is not a `1.0.0` stability declaration. Manual rendered-browser accessibility checks and release-owner acceptance are required before final publication.

## Scope

The release candidate consolidates already-merged capabilities rather than introducing unrelated features:

- Streamlit sample, official Census AIES snapshot, upload, BEA, and Census ASM workflows;
- Explore, Compare, and Scenario Lab analysis;
- canonical `/v1` analytical and metadata API contracts with documented legacy aliases;
- typed source, cache, evaluation, scenario, API, and export lineage;
- privacy-safe `user-upload` provenance;
- CSV, JSON, XLSX, and CSV-lineage companion exports;
- truthful no-auth public-data catalog and typed `/v1/meta/public-data` endpoint;
- protected hosted quality gates and production Docker smoke validation.

Out of scope:

- metric or heuristic formula changes;
- provider expansion;
- dependency churn unrelated to a release blocker;
- API v2 or compatibility changes;
- workflow or Docker redesign;
- promotion to stable `1.0.0`.

## Interrupted acceptance recovery — 2026-07-24

A local Codex acceptance session was interrupted by a system error before it could complete the browser matrix, commit evidence, or push its local changes.

Confirmed local safety and environment evidence before interruption:

- protected primary checkout remained on `architecture/v1-lineage-contract` at `c44cfeaa71e59545ac28be6df58410e3668ecb02`;
- the existing 24-entry dirty inventory was captured and left untouched;
- both protected stashes were captured and left untouched;
- isolated worktree `C:\tmp\app-industry-resilience-v0.2.0rc1` was created from exact candidate `9bf85d5054d01b028a2df05c28e281ee6b98b6e6`;
- Python `3.13.7`, pip `26.1.2`, and Edge `150.0.4078.83` were recorded;
- dependency installation and `pip check` passed;
- GNU Make and Docker were unavailable locally and were not treated as blockers;
- local `pip-audit` was not run because the environment required separate approval for external dependency-metadata disclosure; hosted audit evidence remains authoritative.

Local automated validation completed before browser work:

- Black: 143 files passed;
- Ruff: passed;
- mypy: 101 source files passed;
- protected-module pytest/coverage: `361 passed`, `87.42%` runtime coverage;
- full-source pytest/coverage: `361 passed`, `82.20%` informational coverage;
- independent quiet pytest: `361 passed`, no warnings reported;
- benchmark: passed;
- secret scan: passed;
- `git diff --check`: passed.

Two release-blocking defects were found during live acceptance:

1. The compatibility server generated OpenAPI in-process but returned `404` for live `GET /openapi.json`.
2. Custom Streamlit CSS forced a light application canvas while native controls remained dark-themed; provenance/sidebar contrast was unacceptable, and the translucent state banner became unreadable when the forced canvas was removed.

The recovered branch now contains narrow fixes and regression tests:

- register live `GET /openapi.json` and verify the generated contract;
- stop overriding `.stApp` background so Streamlit owns the active theme;
- make sidebar guidance inherit the active theme;
- use an opaque light-teal state banner with dark text for readable contrast in both appearances;
- add focused API and Streamlit style regressions.

Focused Black, Ruff, mypy, API tests, and Streamlit component tests passed before the recovered fix commit was pushed. The full protected gates and the complete Windows/Edge acceptance matrix must be rerun on the current PR head.

## Resumed acceptance completion — 2026-07-24

Acceptance resumed in clean worktree `C:\tmp\app-industry-resilience-v0.2.0rc1-resume` on local branch `local/v0.2.0rc1-acceptance-resume`. Before testing, the worktree HEAD and upstream both resolved to exact candidate `9b8bc27c79500451414470e4364ed9137ed28d07`, `origin/main` remained `f98962ddd8e6490115cf2ac6b332a9ae86bbea76`, and the primary checkout, its 24-entry dirty inventory, both protected stashes, and the older interrupted worktree were re-recorded without modification.

Protected CI / Quality Gate #229 and protected Docker Smoke #109 passed on exact candidate `9b8bc27c79500451414470e4364ed9137ed28d07`. The resume pass intentionally did not repeat the hosted full gate or local `pip-audit`.

Focused resume validation:

- Python `3.13.7`; pip `26.1.2`; requirements already satisfied; `pip check` reported `No broken requirements found.`;
- Black: exit `0`, four focused files left unchanged;
- Ruff: exit `0`, all focused checks passed;
- focused API/Streamlit regression suite: exit `0`, `31 passed in 6.95s`;
- scenario-lineage, provenance-privacy, and export-integrity suite: exit `0`, `34 passed in 23.58s`;
- live API and Streamlit processes listened only on `127.0.0.1:9000` and `127.0.0.1:8501`;
- `pyproject.toml` project and Commitizen versions, `src/version.py` fallback, and live API `/health`/OpenAPI versions all remained `0.2.0rc1`. The project distribution is not installed as package metadata in this requirements-only virtual environment, so the import used the documented fallback.

## Phase 1 — automated release-candidate evidence

Record results only from the exact pull-request head.

| Gate | Required result | Evidence |
| --- | --- | --- |
| Package version | `pyproject.toml`, Commitizen, and fallback all report `0.2.0rc1` | Passed on initial candidate; unchanged by blocker fixes |
| Black | Pass | Local 143-file pass; focused blocker-fix pass |
| Ruff | Pass | Local full pass; focused blocker-fix pass |
| mypy | Pass | Local 101-file pass; focused blocker-fix pass |
| Full pytest suite | Pass | Local `361 passed`; protected CI #229 passed exact accepted candidate |
| Runtime coverage | At least 85% | Local `87.42%`; protected CI #229 passed exact accepted candidate |
| Full-source coverage | Informational result recorded | Local `82.20%` |
| Metric benchmark | Pass | Local pass |
| Dependency audit | No known vulnerabilities | Protected CI #229 passed exact accepted candidate; local rerun intentionally omitted |
| Secret scan | Pass reviewed baseline | Local pass |
| Hosted CI / Quality Gate | Pass | #222 passed on initial candidate; #229 passed exact accepted candidate `9b8bc27c…` |
| Hosted Docker Deployment Smoke | Pass | #102 passed on initial candidate; #109 passed exact accepted candidate `9b8bc27c…` |
| Candidate diff review | No unrelated feature, provider, dependency, workflow, or Docker changes | Release preparation plus four narrowly scoped blocker-fix files |

## Phase 2 — Windows and Edge manual acceptance

Use a clean isolated worktree or checkout. Do not modify, switch, clean, reset, or stash the existing dirty primary checkout. Both protected stashes must remain untouched.

### Environment record

| Field | Result |
| --- | --- |
| Candidate SHA | Resumed acceptance: `9b8bc27c79500451414470e4364ed9137ed28d07` |
| Windows version | Microsoft Windows 11 Home 25H2, `10.0.26200.8894`, 64-bit |
| Edge version | `150.0.4078.83` |
| Python version | `3.13.7` |
| Install command/result | Passed; requirements satisfied with pip `26.1.2`; `pip check` clean |
| Streamlit URL | `http://127.0.0.1:8501`; loopback-only listener confirmed |
| API URL | `http://127.0.0.1:9000`; loopback-only listener confirmed |

### Dashboard workflows

| Check | Result | Evidence/notes |
| --- | --- | --- |
| First-run opens with Sample (offline) and no credentials | Pass | New session selected Sample, showed 8/8 records, and rendered without exceptions |
| First-run guide dismisses and reopens | Pass | Dismiss removed the guide; sidebar control restored it in the same session |
| Overview and Explore render usable tables | Pass | Overview tables rendered; Explore searches for `Food` and `311` both found Food Manufacturing |
| Compare supports industry selection and table/chart alternatives | Pass | One- and two-industry comparison worked; multi-year upload rendered a Plotly trend plus an operable adjacent data-table expander |
| Scenario Lab runs a non-zero adjustment | Pass | Sample run used +18% gross output and rendered baseline/scenario/delta tables and chart; AIES +1% run also completed |
| Reset scenario returns to idle state | Pass | Reset removed scenario results, restored the explicit idle message, and produced no exception |
| Sample Data provenance is accurate | Pass | `sample` / `bundled_sample` / `sample_industries`, period `2021`, bundled retrieval, sample true, official false, schema/calculation `1` |
| Scenario provenance preserves source and transformation order | Pass | Scenario and delta retained source `sample`; bounded `scenario_adjustment` was followed by `compute_metrics` and `compute_health_scores`; baseline values were byte-for-byte unchanged |
| Full/current CSV downloads work | Pass | Browser-downloaded AIES full CSV was 29,034 bytes and parsed as 87 rows; production helper full/current CSVs were 366/234 bytes and parsed as 4/2 rows |
| CSV lineage companions work | Pass | Full/current companions were 529/533 bytes with `export_serialization` details `csv`, `full`/`filtered`, and 4/2 records |
| JSON exports contain top-level `lineage` and `records` | Pass | Full/current JSONs were 1,409/1,003 bytes, parsed as 4/2 records, and carried the correct export step |
| XLSX contains `Cost Structure` and `Lineage` sheets | Pass | Full/current workbooks were 6,645/6,580 bytes; ZIP/XML parse succeeded, both sheets existed, and lineage scope/record counts were correct |
| Census AIES snapshot vintage/provenance is accurate | Pass | Keyless 87-row survey-year 2023 snapshot; release February 26, 2026; operating-expense proxy disclosed; official Census lineage, metrics, scenario, and exports worked |
| Uploaded CSV is labeled `user-upload` | Pass | Single-year period `2023`; multi-year period `mixed`; `source_kind=uploaded_file`, upload retrieval, sample/official false |
| Uploaded filename/path is absent from provenance and exports | Pass | Filename, path, credential, cache key, Redis URL, raw payload, and arbitrary-attribute sentinels were absent from provenance tables and every export |
| Missing BEA key state is clear and safe | Pass | Password input was empty/masked, requirement text and error were explicit, no ready/fetch state or provider request occurred |
| Missing Census key state is clear and safe | Pass | Password input was empty/masked, requirement text and error were explicit, no ready/fetch state or provider request occurred |
| Invalid upload is rejected with useful guidance | Pass | Missing `year` reported required and optional columns; script-pattern cell reported exact row and column |
| Empty search/filter result is handled clearly | Pass | `ZZZ-NO-MATCH` produced `No industries match the current filters.` |

Export generation was repeated twice. All eight payloads were byte-identical between runs, in-memory lineage remained unchanged, and safe deterministic filenames were:

- `industry_cost_structure_results_full.csv`
- `industry_cost_structure_results_full.lineage.json`
- `industry_cost_structure_results_full.json`
- `industry_cost_structure_results_full.xlsx`
- `industry_cost_structure_results_filtered.csv`
- `industry_cost_structure_results_filtered.lineage.json`
- `industry_cost_structure_results_filtered.json`
- `industry_cost_structure_results_filtered.xlsx`

### API workflows

| Check | Result | Evidence/notes |
| --- | --- | --- |
| `/health` and `/healthz` return acceptable status | Pass | Both HTTP 200; version `0.2.0rc1`; `warn` is the documented no-credential development state |
| `/metrics` returns Prometheus text | Pass | HTTP 200, `text/plain; version=0.0.4`, 80 lines, request counter present |
| OpenAPI document renders | Pass | Live HTTP 200; version `0.2.0rc1`; 20 paths; `/openapi.json` included; no `/v2` paths |
| `/v1/meta/sources` works | Pass | HTTP 200 with `sources` document |
| `/v1/meta/connectors` works | Pass | HTTP 200; three `data_source` connectors |
| `/v1/meta/public-data` distinguishes implemented and roadmap entries | Pass | HTTP 200; 9 entries, 2 implemented, 2 readiness-complete, 7 roadmap; Census ASM absent; GDELT event-context/non-ground-truth |
| `/v1/evaluate` works with inline records and typed lineage | Pass | HTTP 200; metadata retained; typed inline lineage and ordered load/normalize/metric/health steps |
| `/v1/scenario` works with typed lineage | Pass | HTTP 200; metadata retained; bounded adjustment and ordered recalculation steps |
| `/v1/analytics/health` works | Pass | HTTP 200 with health, metadata, and typed lineage |
| Deprecated aliases return migration headers | Pass | Five aliases returned `Deprecation: true`, `Sunset: Fri, 15 Jan 2027 00:00:00 GMT`, and successor `Link`; canonical routes returned none |
| Invalid requests retain documented status/error shapes | Pass | Missing year and `top_n=0` returned 422 field details; empty scenario returned 400; `/v2/evaluate` returned documented 404 |

### Rendered-browser accessibility

| Check | Result | Evidence/notes |
| --- | --- | --- |
| Keyboard-only traversal follows a logical order | Pass | Dedicated Edge tab traversed sidebar, named tabs, Explore search/data controls, Compare, Scenario targets/expander/sliders/run/reset, diagnostics, and full/current downloads |
| Focus is visibly indicated on interactive controls | Pass | Tabs, buttons, links, expanders, chips, and download controls showed the Streamlit focus shadow; data-grid controls exposed their focused tooltip |
| Streamlit tabs expose meaningful names and selected state | Pass | Overview, Explore, Compare, and Scenario Lab exposed `role=tab`; arrow-key changes updated `aria-selected` exactly |
| Loading/success/warning/error states are understandable without color | Pass | Ready, idle, upload success, AIES proxy warning, missing-key errors, and invalid-upload errors all included explicit text and status/alert semantics |
| Available screen reader announces key state changes | NOT RUN | No screen reader was exercised safely; no PASS claimed |
| 200% zoom preserves essential content and controls | Pass | Edge effective 200% viewport was 514 CSS px at DPR 2.5; outer document was 514/514 client/scroll width, tabs and controls remained available, and only the data grid retained scoped horizontal scrolling |
| Light appearance contrast review | Pass | App 12.53:1, sidebar/guidance 11.18:1, state banner 14.75:1 |
| Dark appearance contrast review | Pass | App 18.11:1, sidebar/guidance 14.22:1, state banner 14.75:1; `.stApp` background override absent |
| Plotly charts have usable adjacent tables/text | Pass | Multi-year Edge run rendered Plotly trend and keyboard-opened `View historical trend data table`; scenario chart had adjacent comparison and leading-change tables |

Platform-origin defects in Streamlit or Plotly must be recorded as limitations rather than hidden with fragile DOM scripting.

No release-blocking Streamlit or Plotly platform limitation was observed. Screen-reader coverage remains NOT RUN. Streamlit `1.60.0` logged non-blocking repository deprecation warnings for `use_container_width` and one session-state/default warning for `scenario_target_codes`; no rendered exception or core-use failure resulted.

## Phase 2 release-owner decision

- [x] **GO:** all release-blocking checks pass; proceed to final `0.2.0` version commit.
- [ ] **CONDITIONAL GO:** only documented non-blocking platform limitations remain.
- [ ] **NO-GO:** one or more release-blocking defects remain.

Decision date: 2026-07-24<br>
Decision owner: Acceptance recommendation recorded by Codex; final release-owner approval remains required<br>
Decision notes: GO recommended. Protected CI #229 and Docker Smoke #109 passed exact accepted candidate `9b8bc27c…`; focused recovery, complete dashboard/API/export/provenance, and dedicated Edge accessibility matrices passed. Screen reader was not run and no PASS was claimed. No new release-blocking defect was found.

## Phase 3 — final publication checklist

Do not execute these steps before a GO or accepted CONDITIONAL GO decision.

- [ ] Change package, Commitizen, and fallback versions from `0.2.0rc1` to `0.2.0`.
- [ ] Replace candidate placeholders with exact manual and hosted validation results.
- [ ] Consolidate `v0.2.0` release notes.
- [ ] Run fresh protected CI and Docker checks on the exact final release head.
- [ ] Squash-merge the release PR with an exact-head guard.
- [ ] Verify merged `main` equals the expected release SHA.
- [ ] Create immutable annotated tag `v0.2.0` at that SHA.
- [ ] Create the GitHub release classified as Public Beta.
- [ ] Verify the tag and release resolve to the same commit.
- [ ] Move issue #107 from active TASKLIST work into completed history.

## Final release evidence

| Field | Result |
| --- | --- |
| Final package version | Pending |
| Final merged main SHA | Pending |
| Final CI run | Pending |
| Final Docker Smoke run | Pending |
| Annotated tag | Pending |
| GitHub release | Pending |
| Tag/release SHA parity | Pending |
