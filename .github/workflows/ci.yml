name: CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  quality-gate:
    uses: ./.github/workflows/quality-gate.yml
    with:
      python-version: "3.11"
    secrets: inherit
